spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/proyecto_megatech
    username: patricio.celi
    password: Marias6681721.
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  sql:
    init:
      mode: always

server:
  port: 8080