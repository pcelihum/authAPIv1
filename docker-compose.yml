version: '3.8'
services:
  postgres:
    image: postgres:16-alpine
    container_name: postgres-sq15
    environment:
      POSTGRES_USER: 
      POSTGRES_PASSWORD: 
      POSTGRES_DB: proyecto_megatech
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
